/*
import React from "react";
import {
    Text,
    StyleSheet,
    ViewProps,
    TouchableOpacity,
  } from 'react-native';
  import { colors, ColorVariants } from '../../constants/colors';
  import { Image } from 'expo-image';

  type CourseProps = ViewProps & {
    variant?: string;
    icon?: any;
    active?: boolean;
    onPress?: () => void;
  };

  const CourseCard({children, variant, icon, onPress, active} : CourseProps) => {
        
  };

  export default CourseCard;

*/